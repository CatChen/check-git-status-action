name: GraphQL Schema

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  graphql-schema:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "GraphQL schema workflow is scheduled hourly."
